width = float(input('Type the width: '))
height = float(input('Type the height: '))
area = width * height
ink = area / 2
print('The area is {} meters, and {} liters of ink is necessary.'.format(area, ink))
